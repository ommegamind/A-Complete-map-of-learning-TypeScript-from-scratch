// learning type annotation and type inference.
// Annotation: is when types need to be explicitely mentioned.
// Inference: when compiler itself figures out the type a file should be.
//example:

//inference

export let springRole = "non-veg";

//Annotation

export let name: string;

name = "omgarg";
